// Width and Height of processed image icon (not the display size)
export const iconSize = 125;

// Size of processed image in cloudinary (not the display size)
export const imageWidth = 500;
export const imageHeight = Math.round(imageWidth * (2 / 3)); // 333
